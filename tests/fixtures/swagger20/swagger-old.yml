swagger: '2.0'
info:
  title: Cross Version Fixture
  version: 1.0.0
host: api.example.com
basePath: /v1
schemes:
  - https
produces:
  - application/json
paths:
  /orders:
    get:
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/Order'
definitions:
  Order:
    type: object
    required:
      - id
    properties:
      id:
        type: string
      total:
        type: number
        minimum: 0
